#!/usr/bin/env ruby1.9
# Compile and upload content

UPLOAD_TO = 'sitnik@sitnik.ru:/home/sitnik/data/www/sitnik.ru/'

require 'pathname'
load Pathname.new(__FILE__).dirname.join('build')

exec "rsync --recursive --delete --compress --progress --human-readable #{PUBLIC}/ #{UPLOAD_TO}"
